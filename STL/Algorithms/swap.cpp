#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
    int a,b;
    a=4;
    b=6;
    swap(a,b);
    cout << "Now a is:" << a << endl;
    cout << "Now b is:" << b << endl;
    
}